language: python
python:
  - "2.7"
addons:
  postgresql: '9.4'

install:
    - pip install -r requirements.txt
    - yes | python setup.py install

before_script:
    - python bin/destroy_and_setup_psqlgraph.py
    - pip freeze

script: "py.test -v"
